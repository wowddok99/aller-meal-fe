# Server-only backend origin used by the Next.js same-origin API proxy.
API_ORIGIN=http://localhost:8080

# OpenAPI source used when regenerating Orval clients. Do not expose this as NEXT_PUBLIC_*.
OPENAPI_ORIGIN=http://localhost:8080
